{
  "schemaVersion": 1,
  "note": "Per-feed last-seen entry IDs. Populated by .github/workflows/model-release-watch.yml (Phase 2 / Gate 8, issue #102). Empty on first deploy; primes itself on the first scheduled run without opening intake issues.",
  "feeds": {}
}
