// Loader for /recommend/between (issue #97 / Gate 3).
//
// All ranking happens client-side via $lib/analyses/recommender. The
// shared parent loader supplies the catalog snapshot.

export const prerender = true;
